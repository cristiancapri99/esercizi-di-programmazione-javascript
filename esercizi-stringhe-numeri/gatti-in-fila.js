var cats = 44;

var inLine = 6;

var file = Math.ceil (cats/inLine);

 var missing = file*inLine-cats;

console.log((cats) in fila per (inLine) si unirono compatti in (file). Nell'ultima fila rimasero (missing) posti gatto disponibili);
    
